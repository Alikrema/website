{
  "name": "ali-krema",
  "version": "2.0.0",
  "private": true,
  "description": "The personal site of Ali Krema.",
  "scripts": {
    "build": "eleventy",
    "build:sites": "npm run build && node scripts/build-sites.mjs",
    "dev": "eleventy --serve",
    "test": "npm run build && node tests/window-viewport.test.mjs && node tests/reader-window-viewport.test.mjs && node tests/site-metadata.test.mjs"
  },
  "author": "Ali Krema",
  "license": "UNLICENSED",
  "dependencies": {
    "@11ty/eleventy": "^3.1.0"
  }
}
